import { createTheme } from "@mui/material";

export const envogueTheme = createTheme({
  palette: {
    primary: {
      main: "#212529",
    },
    secondary: {
      main: "#fff",
    },
  },
  typography: {
    fontFamily: "Montserrat Alternates",
  },
});
